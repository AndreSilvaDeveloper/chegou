# Frontend em modo DEV (Vite dev server + HMR).
# O código é montado por bind mount no docker-compose; o node_modules fica no container.
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
